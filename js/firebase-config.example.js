// Copy this file to `js/firebase-config.js` and replace the placeholders with your Firebase web app config.
// Then include the file in your pages (add <script src="js/firebase-config.js"></script> before discovery-service.js)

// Example:
// window.FIREBASE_CONFIG = {
//   apiKey: "AIza...",
//   authDomain: "your-project.firebaseapp.com",
//   projectId: "your-project-id",
//   storageBucket: "your-project.appspot.com",
//   messagingSenderId: "1234567890",
//   appId: "1:1234567890:web:abcdef",
// };

// For security, keep this file out of public repos if it contains sensitive settings. For GitHub Pages you can still embed the Firebase config (it's public by design),
// but ensure your Firestore rules only allow authenticated access where appropriate.

window.FIREBASE_CONFIG = null; // replace null with your config object when ready
